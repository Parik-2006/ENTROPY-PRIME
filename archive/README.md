# archive/ — Experimental Snapshots

Destination for files classified as **EXPERIMENTAL** (patches, ad-hoc test
scripts, per-developer design specs, one-off Dockerfiles). Preserved verbatim;
never deleted.

**Status at the facade stage:** nothing physically moved here yet; candidates
are classified in place in `../MIGRATION_LOG.md`.

See also `../migration_backup/` for a full pre-migration snapshot of `backend/`
and `src/services/` used for rollback.
