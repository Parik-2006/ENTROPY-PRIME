# DEMO 3 — Different Human Detection

A friend uses the **same account, laptop, and browser**. The Continuous-Auth
engine detects the behavioral mismatch and drops trust into the RED zone
(force_logout / re-auth).

```bash
python examples/different_human_demo/demo.py
```
